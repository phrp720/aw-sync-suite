## This is the configuration file for the agent
Settings: ## Settings for the agent
  aw-url: <activitywatch_url>
  prometheus-url: <prometheus_url>
  excluded-watchers:
    - <watcher_name>
  userId: <user_identification>
  prometheus-secret-key:
  include-hostname: true

## Write the filters here
#Filters: